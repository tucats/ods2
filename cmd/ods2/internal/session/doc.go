// Package session implements the ods2 command interpreter: session state
// (current default file spec, mounted volumes, verbosity, version-delimiter
// character) and the command table shared by both the interactive REPL and
// the one-shot CLI.
package session
